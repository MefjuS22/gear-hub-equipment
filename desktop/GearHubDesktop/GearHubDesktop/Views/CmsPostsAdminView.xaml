<views:ViewControllerBase x:Class="GearHubDesktop.Views.CmsPostsAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:GearHubDesktop.Views">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Portal content" Style="{StaticResource PageTitleText}" />
            <TextBlock Text="News articles for the public site."
                       Style="{StaticResource PageSubtitleText}"
                       Margin="0,6,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,12,0">
                    <Button Content="News posts" Style="{StaticResource SegmentTabButtonActive}" Margin="0,0,8,0" />
                    <Button Content="Portal texts" Style="{StaticResource CompactSecondaryButton}" Click="PortalTexts_Click" />
                </StackPanel>
                <TextBox Grid.Column="1"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,12,0" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Refresh" Style="{StaticResource CompactSecondaryButton}" Margin="0,0,8,0" Click="Refresh_Click" />
                    <Button Content="New post" Style="{StaticResource CompactPrimaryButton}" Click="Add_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="{Binding ErrorMessage}"
                       Style="{StaticResource ErrorText}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />
            <TextBlock Text="{Binding StatusMessage}"
                       Style="{StaticResource SuccessText}"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />
        </StackPanel>

        <Border Grid.Row="3" Style="{StaticResource CardFlat}" Padding="0">
            <DataGrid ItemsSource="{Binding Posts}"
                      SelectedItem="{Binding SelectedPost}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" MinWidth="160" />
                    <DataGridTextColumn Header="Slug" Binding="{Binding Slug}" Width="180" />
                    <DataGridTextColumn Header="Published" Binding="{Binding PublishedLabel}" Width="90" />
                    <DataGridTemplateColumn Header="Actions" Width="180" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                            Style="{StaticResource CompactSecondaryButton}"
                                            Margin="0,0,8,0"
                                            Click="EditRow_Click" />
                                    <Button Content="Delete"
                                            Style="{StaticResource CompactSecondaryButton}"
                                            Click="DeleteRow_Click" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3"
                Style="{StaticResource LoadingOverlay}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="{StaticResource TextSecondaryBrush}" />
        </Border>
    </Grid>
</views:ViewControllerBase>
