<views:ViewControllerBase x:Class="GearHubDesktop.Views.PortalTextsAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:GearHubDesktop.Views">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Portal content" Style="{StaticResource PageTitleText}" />
            <TextBlock Text="Catalog, cart, and news page copy."
                       Style="{StaticResource PageSubtitleText}"
                       Margin="0,6,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,12,0">
                    <Button Content="News posts" Style="{StaticResource CompactSecondaryButton}" Margin="0,0,8,0" Click="NewsPosts_Click" />
                    <Button Content="Portal texts" Style="{StaticResource SegmentTabButtonActive}" />
                </StackPanel>
                <TextBox Grid.Column="1"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,12,0" />
                <Button Grid.Column="2"
                        Content="Refresh"
                        Style="{StaticResource CompactSecondaryButton}"
                        Click="Refresh_Click" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="{Binding ErrorMessage}"
                       Style="{StaticResource ErrorText}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />
            <TextBlock Text="{Binding StatusMessage}"
                       Style="{StaticResource SuccessText}"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />
        </StackPanel>

        <Border Grid.Row="3" Style="{StaticResource CardFlat}" Padding="0">
            <DataGrid ItemsSource="{Binding Texts}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Label" Binding="{Binding Title}" Width="180" />
                    <DataGridTextColumn Header="Placement" Binding="{Binding PlacementHint}" Width="220" />
                    <DataGridTextColumn Header="Preview" Binding="{Binding Preview}" Width="*" MinWidth="200" />
                    <DataGridTemplateColumn Header="Actions" Width="100" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Edit"
                                        Style="{StaticResource CompactSecondaryButton}"
                                        Click="EditRow_Click" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3"
                Style="{StaticResource LoadingOverlay}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="{StaticResource TextSecondaryBrush}" />
        </Border>
    </Grid>
</views:ViewControllerBase>
