<views:ViewControllerBase x:Class="GearHubDesktop.Views.PortalTextsAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:GearHubDesktop.Views">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Portal content" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="Catalog, cart, and news page copy."
                       Foreground="#666666"
                       Margin="0,4,0,0" />
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Refresh" Style="{StaticResource BusyAwareButton}" Padding="12,6" Click="Refresh_Click" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" Margin="0,0,12,0">
                <Button Content="News posts" Style="{StaticResource BusyAwareButton}" Padding="12,6" Margin="0,0,8,0" Click="NewsPosts_Click" />
                <Button Content="Portal texts" Padding="12,6" FontWeight="SemiBold" IsEnabled="False" />
            </StackPanel>
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="6,4"
                     Width="280"
                     VerticalContentAlignment="Center" />
        </DockPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="IndianRed" TextWrapping="Wrap" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="SeaGreen" />
        </StackPanel>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Texts}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Label" Binding="{Binding Title}" Width="180" />
                <DataGridTextColumn Header="Placement" Binding="{Binding PlacementHint}" Width="220" />
                <DataGridTextColumn Header="Preview" Binding="{Binding Preview}" Width="*" MinWidth="200" />
                <DataGridTemplateColumn Header="Actions" Width="100" CanUserSort="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit"
                                    Margin="4,0"
                                    Padding="10,4"
                                    Click="EditRow_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16" />
        </Border>
    </Grid>
</views:ViewControllerBase>
