<views:ViewControllerBase x:Class="GearHubDesktop.Views.OrderDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:GearHubDesktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="900">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <Style x:Key="BusyAwareButton" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </views:ViewControllerBase.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="{Binding HeaderText}"
                       FontSize="22"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <Button Content="Export PDF"
                    Style="{StaticResource BusyAwareButton}"
                    DockPanel.Dock="Right"
                    Padding="12,6"
                    Click="ExportPdf_Click" />
        </DockPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="{Binding CustomerLine}" />
            <TextBlock Text="{Binding UserLine}" />
            <TextBlock Text="{Binding OrderDateLine}" />
            <TextBlock Text="{Binding RentalPeriodLine}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="IndianRed" TextWrapping="Wrap" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="SeaGreen" />
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Text="Line items"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8" />

        <DataGrid Grid.Row="4"
                  ItemsSource="{Binding LineItems}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Equipment ID" Binding="{Binding EquipmentId}" Width="100" />
                <DataGridTextColumn Header="Equipment" Binding="{Binding EquipmentName}" Width="*" MinWidth="160" />
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="70" />
                <DataGridTextColumn Header="Unit price" Binding="{Binding UnitPrice, StringFormat=C}" Width="110" />
                <DataGridTextColumn Header="Line total" Binding="{Binding LineTotalDisplay}" Width="110" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="4"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16" />
        </Border>
    </Grid>
</views:ViewControllerBase>
