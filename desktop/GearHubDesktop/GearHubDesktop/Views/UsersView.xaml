<views:ViewControllerBase x:Class="GearHubDesktop.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:GearHubDesktop.Views">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <Style x:Key="BusyAwareButton" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </views:ViewControllerBase.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Users" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12" />

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" MaxWidth="640" HorizontalAlignment="Left">
            <StackPanel>
                <TextBlock Text="Create user" FontWeight="SemiBold" Margin="0,0,0,12" />
                <TextBlock Text="Email" />
                <TextBox Text="{Binding NewEmail, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Margin="0,4,0,8" />
                <TextBlock Text="Password" />
                <PasswordBox x:Name="NewPasswordInput" Padding="8,6" Margin="0,4,0,8" />
                <TextBlock Text="Display name" />
                <TextBox Text="{Binding NewDisplayName, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Margin="0,4,0,8" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <CheckBox Content="Admin" IsChecked="{Binding AssignAdminRole}" Margin="0,0,16,0" />
                    <CheckBox Content="User" IsChecked="{Binding AssignUserRole}" />
                </StackPanel>
                <Button Content="Create user" Style="{StaticResource BusyAwareButton}" Padding="12,8" HorizontalAlignment="Left" Click="CreateUser_Click" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="IndianRed" TextWrapping="Wrap" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="SeaGreen" />
        </StackPanel>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Users}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" MinWidth="180" />
                <DataGridTextColumn Header="Display name" Binding="{Binding DisplayName}" Width="180" />
                <DataGridTextColumn Header="Roles" Binding="{Binding RolesDisplay}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
        </Border>
    </Grid>
</views:ViewControllerBase>
