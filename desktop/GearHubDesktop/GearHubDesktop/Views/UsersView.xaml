<views:ViewControllerBase x:Class="GearHubDesktop.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:GearHubDesktop.Views">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Users" Style="{StaticResource PageTitleText}" />
            <TextBlock Text="Staff accounts, roles, and access permissions."
                       Style="{StaticResource PageSubtitleText}"
                       Margin="0,6,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add user" Style="{StaticResource CompactPrimaryButton}" Margin="0,0,8,0" Click="AddUser_Click" />
                <Button Content="Edit roles" Style="{StaticResource CompactSecondaryButton}" Margin="0,0,8,0" Click="EditRoles_Click" IsEnabled="{Binding CanEditRoles}" />
                <Button Content="Delete" Style="{StaticResource CompactSecondaryButton}" Margin="0,0,8,0" Click="DeleteUser_Click" IsEnabled="{Binding CanDeleteSelectedUser}" />
                <Button Content="Refresh" Style="{StaticResource CompactSecondaryButton}" Click="Refresh_Click" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="{Binding ErrorMessage}"
                       Style="{StaticResource ErrorText}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />
            <TextBlock Text="{Binding StatusMessage}"
                       Style="{StaticResource SuccessText}"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />
        </StackPanel>

        <Border Grid.Row="3" Style="{StaticResource CardFlat}" Padding="0">
            <DataGrid ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" MinWidth="180" />
                    <DataGridTextColumn Header="Display name" Binding="{Binding DisplayName}" Width="180" />
                    <DataGridTextColumn Header="Roles" Binding="{Binding RolesDisplay}" Width="200" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3"
                Style="{StaticResource LoadingOverlay}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="{StaticResource TextSecondaryBrush}" />
        </Border>
    </Grid>
</views:ViewControllerBase>
