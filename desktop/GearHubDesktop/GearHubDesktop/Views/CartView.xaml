<views:ViewControllerBase x:Class="GearHubDesktop.Views.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:GearHubDesktop.Views"
             mc:Ignorable="d">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <views:InverseBooleanConverter x:Key="InverseBool" />
        <views:NullOrEmptyToVisibilityConverter x:Key="NullToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="Refresh"
                        Style="{StaticResource BusyAwareButton}"
                        Padding="12,6"
                        VerticalAlignment="Top"
                        Click="Refresh_Click" />
                <StackPanel>
                    <TextBlock Text="Cart" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="Review items and place your rental order."
                               Style="{StaticResource PageSubtitleText}"
                               Margin="0,6,0,0" />
                </StackPanel>
            </DockPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardFlat}" Padding="0" Margin="0,0,16,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{Binding ErrorMessage}"
                               Style="{StaticResource ErrorText}"
                               Margin="16,16,16,0"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Lines}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="0"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              Margin="0,8,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Item" Binding="{Binding Name}" Width="*" MinWidth="160" />
                            <DataGridTextColumn Header="Daily rate" Binding="{Binding DailyRate, StringFormat=C}" Width="100" />
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60" />
                            <DataGridTemplateColumn Header="" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="-"
                                                    Width="32"
                                                    Style="{StaticResource SecondaryButton}"
                                                    Margin="4,0,0,0"
                                                    Padding="0"
                                                    Click="DecreaseQuantity_Click" />
                                            <Button Content="+"
                                                    Width="32"
                                                    Style="{StaticResource SecondaryButton}"
                                                    Margin="4,0,0,0"
                                                    Padding="0"
                                                    Click="IncreaseQuantity_Click" />
                                            <Button Content="Remove"
                                                    Style="{StaticResource GhostButton}"
                                                    Margin="4,0,0,0"
                                                    Padding="8,4"
                                                    Click="RemoveLine_Click" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="1"
                               Text="Your cart is empty."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#666666"
                               Visibility="{Binding IsCartEmpty, Converter={StaticResource BoolToVisibility}}" />
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Checkout"
                               FontWeight="SemiBold"
                               FontSize="17"
                               Margin="0,0,0,18" />

                    <TextBlock Text="Saved company" Style="{StaticResource FormLabelText}" />
                    <ComboBox ItemsSource="{Binding SavedCompanies}"
                              SelectedItem="{Binding SelectedCompany}"
                              DisplayMemberPath="CompanyName"
                              Padding="8,6"
                              Margin="0,0,0,12" />

                    <TextBlock Text="Company name" Style="{StaticResource FormLabelText}" />
                    <TextBox Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,14" />

                    <TextBlock Text="Contact person" Style="{StaticResource FormLabelText}" />
                    <TextBox Text="{Binding ContactPerson, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,14" />

                    <TextBlock Text="Rental start" Style="{StaticResource FormLabelText}" />
                    <DatePicker SelectedDate="{Binding RentalStartDate}"
                                Margin="0,0,0,12" />

                    <TextBlock Text="Rental end" Style="{StaticResource FormLabelText}" />
                    <DatePicker SelectedDate="{Binding RentalEndDate}"
                                Margin="0,0,0,16" />

                    <TextBlock Text="{Binding SubtotalLabel}"
                               FontWeight="SemiBold"
                               Margin="0,0,0,16" />

                    <Button Content="Place order"
                            Padding="12,8"
                            FontWeight="SemiBold"
                            Click="PlaceOrder_Click"
                            IsEnabled="{Binding CanPlaceOrder}" />

                    <TextBlock Text="{Binding StatusMessage}"
                               Style="{StaticResource SuccessText}"
                               Margin="0,12,0,0"
                               Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />

                    <TextBlock Text="Sign in to place an order."
                               Style="{StaticResource PageSubtitleText}"
                               Margin="0,12,0,0"
                               Visibility="{Binding ShowSignInHint, Converter={StaticResource BoolToVisibility}}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</views:ViewControllerBase>
