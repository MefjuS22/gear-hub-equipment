<views:ViewControllerBase x:Class="GearHubDesktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:GearHubDesktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20" LastChildFill="False">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Dashboard" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="Overview of orders, equipment, and activity."
                           Style="{StaticResource PageSubtitleText}"
                           Margin="0,6,0,0" />
            </StackPanel>
            <Button Content="Export Excel"
                    Style="{StaticResource CompactSecondaryButton}"
                    DockPanel.Dock="Right"
                    VerticalAlignment="Center"
                    Click="ExportExcel_Click" />
        </DockPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="{Binding ErrorMessage}"
                       Style="{StaticResource ErrorText}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />
            <TextBlock Text="{Binding StatusMessage}"
                       Style="{StaticResource SuccessText}"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Total orders" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.TotalOrders}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Orders (30 days)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.OrdersLast30Days}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Customers" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.TotalCustomers}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Equipment" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.TotalEquipment}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Available equipment" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.AvailableEquipment}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Logins (24h)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.LoginsLast24Hours}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Unique users (24h)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Summary.UniqueUsersLoggedInLast24Hours}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}" Margin="0,0,14,14">
                    <StackPanel>
                        <TextBlock Text="Revenue (30 days)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                        <TextBlock Text="{Binding EstimatedRevenueDisplay}" FontSize="28" FontWeight="Bold" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>
            </WrapPanel>
        </ScrollViewer>

        <Border Grid.Row="2"
                Style="{StaticResource LoadingOverlay}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="{StaticResource TextSecondaryBrush}" />
        </Border>
    </Grid>
</views:ViewControllerBase>
