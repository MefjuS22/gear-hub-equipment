<views:ViewControllerBase x:Class="GearHubDesktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:GearHubDesktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <views:ViewControllerBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <Style x:Key="KpiCardStyle" TargetType="Border">
            <Setter Property="Width" Value="180" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="BorderBrush" Value="#DDDDDD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Background" Value="White" />
        </Style>
        <Style x:Key="BusyAwareButton" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </views:ViewControllerBase.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Dashboard"
                       FontSize="22"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <Button Content="Export Excel"
                    Style="{StaticResource BusyAwareButton}"
                    DockPanel.Dock="Right"
                    Padding="12,6"
                    Click="ExportExcel_Click" />
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="IndianRed"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8" />

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Foreground="SeaGreen"
                   Margin="0,0,0,8" />

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <WrapPanel>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Total orders" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.TotalOrders}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Orders (30 days)" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.OrdersLast30Days}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Customers" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.TotalCustomers}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Equipment" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.TotalEquipment}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Available equipment" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.AvailableEquipment}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Logins (24h)" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.LoginsLast24Hours}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Unique users (24h)" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding Summary.UniqueUsersLoggedInLast24Hours}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="Revenue (30 days)" Foreground="Gray" FontSize="12" />
                        <TextBlock Text="{Binding EstimatedRevenueDisplay}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </WrapPanel>
        </ScrollViewer>

        <Border Grid.Row="3"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Loading…"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16" />
        </Border>
    </Grid>
</views:ViewControllerBase>
