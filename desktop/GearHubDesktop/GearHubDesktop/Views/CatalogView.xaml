<views:ViewControllerBase x:Class="GearHubDesktop.Views.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:GearHubDesktop.Views"
             mc:Ignorable="d">
    <views:ViewControllerBase.Resources>
        <views:InverseBooleanConverter x:Key="InverseBool" />
        <views:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibility" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <views:NullOrEmptyToVisibilityConverter x:Key="NullToVisibility" />
    </views:ViewControllerBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="Refresh"
                        Style="{StaticResource CompactSecondaryButton}"
                        VerticalAlignment="Top"
                        Click="Refresh_Click" />
                <StackPanel>
                    <TextBlock Text="Equipment catalog" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="Browse available gear and add items to your cart."
                               Style="{StaticResource PageSubtitleText}"
                               Margin="0,6,0,0" />
                </StackPanel>
            </DockPanel>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Search" Style="{StaticResource FormLabelText}" />
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Category" Style="{StaticResource FormLabelText}" />
                    <ComboBox ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding SelectedCategory}" />
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Search"
                        Style="{StaticResource CompactPrimaryButton}"
                        VerticalAlignment="Bottom"
                        Click="Search_Click" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Style="{StaticResource ErrorText}"
                   Margin="0,0,0,12"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />

        <Border Grid.Row="3" Style="{StaticResource CardFlat}" Padding="0">
            <DataGrid ItemsSource="{Binding Items}"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="" Width="72" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Width="48" Height="48" Margin="4">
                                    <Border Background="#F1F5F9"
                                            CornerRadius="8"
                                            Visibility="{Binding HasThumbnail, Converter={StaticResource InverseBoolToVisibility}, FallbackValue=Visible}" />
                                    <TextBlock Text="—"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               Visibility="{Binding HasThumbnail, Converter={StaticResource InverseBoolToVisibility}}" />
                                    <Border CornerRadius="8"
                                            ClipToBounds="True"
                                            Visibility="{Binding HasThumbnail, Converter={StaticResource BoolToVisibility}}">
                                        <Image Source="{Binding Thumbnail}"
                                               Width="48"
                                               Height="48"
                                               Stretch="UniformToFill" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="160" />
                    <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="130" />
                    <DataGridTextColumn Header="Brand" Binding="{Binding BrandName}" Width="130" />
                    <DataGridTextColumn Header="Daily rate" Binding="{Binding DailyRate, StringFormat=C}" Width="110" />
                    <DataGridTextColumn Header="Available" Binding="{Binding IsAvailable}" Width="90" />
                    <DataGridTemplateColumn Header="Actions" Width="200" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Open"
                                            Style="{StaticResource CompactSecondaryButton}"
                                            Margin="0,0,8,0"
                                            Click="Open_Click" />
                                    <Button Content="Add to cart"
                                            Style="{StaticResource CompactPrimaryButton}"
                                            Click="AddToCart_Click"
                                            IsEnabled="{Binding IsAvailable}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="14" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Text="{Binding PaginationLabel}" />

                <Button Grid.Column="1"
                        Content="Previous"
                        Style="{StaticResource CompactSecondaryButton}"
                        Margin="0,0,8,0"
                        Click="PreviousPage_Click"
                        IsEnabled="{Binding CanGoPrevious}" />

                <Button Grid.Column="2"
                        Content="Next"
                        Style="{StaticResource CompactSecondaryButton}"
                        Margin="0,0,16,0"
                        Click="NextPage_Click"
                        IsEnabled="{Binding CanGoNext}" />

                <TextBlock Grid.Column="3"
                           VerticalAlignment="Center"
                           Style="{StaticResource SuccessText}"
                           Text="{Binding StatusMessage}"
                           Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />
            </Grid>
        </Border>
    </Grid>
</views:ViewControllerBase>
